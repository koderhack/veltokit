import Foundation

/// Reprezentuje typ `QuizRules`.
enum QuizRules {
/// Przechowuje wartosc `questionsPerRound`.
  static let questionsPerRound = 10
/// Przechowuje wartosc `categoryChoicesPerRound`.
  static let categoryChoicesPerRound = 4
/// Przechowuje wartosc `soloRounds`.
  static let soloRounds = 5
/// Przechowuje wartosc `duoRounds`.
  static let duoRounds = 20
/// Przechowuje wartosc `feedbackDuration`.
  static let feedbackDuration: TimeInterval = 1.0
}
