import {DOC_PATHS} from '@site/siteRoutes';

export const GAME_DOC_PATHS = {
  pong: DOC_PATHS.examplePong,
  dart: DOC_PATHS.exampleDart,
  bowling: DOC_PATHS.exampleBowling,
  quiz: DOC_PATHS.exampleQuiz,
} as const;
